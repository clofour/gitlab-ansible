{
    "name": "Debian",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "remoteUser": "dev",
    "postCreateCommand": "chmod -R o-w /workspaces/${localWorkspaceFolderBasename} && chmod -R u+rwX,go= /home/dev/.ssh",
    "mounts": [
        // Cross-platform home directory: one of the two environment variables will always be empty
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.ssh,target=/home/dev/.ssh,type=bind,consistency=cached",
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.secrets,target=/home/dev/.secrets,type=bind,consistency=cached",
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.keys,target=/home/dev/.keys,type=bind,consistency=cached"
    ]
}
